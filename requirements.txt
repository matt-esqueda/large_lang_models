# Dependencies are declared in pyproject.toml; this installs the package editable.
# PyTorch is deliberately unpinned here. Install it FIRST, separately, using
# the index that matches your hardware:
#
#   CUDA 12.8 (RTX 50-series):
#     pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cu128
#   CUDA 12.x (older NVIDIA):
#     pip install torch
#   CPU-only:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu

-e .[dev]
